pandas>=2.0
numpy>=1.24
requests>=2.28
ccxt>=4.0
scikit-learn>=1.3
pyarrow>=15.0
# collector.py uses ccxt.pro websockets; aiohttp/aiodns come in via ccxt today
# but are pinned explicitly so the tick pipeline can't silently break if a
# future ccxt makes async an extra.
aiohttp>=3.10
aiodns>=3.0
